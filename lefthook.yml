pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,ts,json}"
      run: bunx biome check --write --unsafe {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}
